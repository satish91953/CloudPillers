# ============================================
# CloudPillers Backend - Environment Variables
# ============================================
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# ============================================
# Server Configuration
# ============================================
NODE_ENV=development
PORT=5001

# ============================================
# Database Configuration (MongoDB)
# ============================================
# Local MongoDB
MONGODB_URI=mongodb://admin:changeme@localhost:27017/cloudpillers?authSource=admin

# MongoDB Atlas (Cloud) - Alternative
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/cloudpillers?retryWrites=true&w=majority

# ============================================
# JWT Authentication
# ============================================
JWT_SECRET=your_super_secret_jwt_key_change_in_production
JWT_EXPIRE=7d

# ============================================
# AWS SES Email Configuration
# ============================================
# AWS Region where SES is configured
AWS_REGION=us-east-1

# AWS IAM User Access Keys (for SES)
# Get these from AWS IAM Console after creating a user with SES permissions
AWS_ACCESS_KEY_ID=your_aws_access_key_id
AWS_SECRET_ACCESS_KEY=your_aws_secret_access_key

# Email Configuration
# EMAIL_FROM must be a verified email address or domain in AWS SES
EMAIL_FROM=noreply@cloudpillers.com

# Admin email to receive notifications
ADMIN_EMAIL=admin@cloudpillers.com

# ============================================
# Cloudinary (Image Upload Service)
# ============================================
# Optional - Only needed if you want to upload images
# Get these from https://cloudinary.com/
CLOUDINARY_CLOUD_NAME=your_cloud_name
CLOUDINARY_API_KEY=your_api_key
CLOUDINARY_API_SECRET=your_api_secret

# ============================================
# Client/Frontend Configuration
# ============================================
# Frontend URL (for CORS and email links)
CLIENT_URL=http://localhost:3001

# ============================================
# Rate Limiting
# ============================================
# Rate limit window in milliseconds (default: 15 minutes)
RATE_LIMIT_WINDOW_MS=900000

# Maximum requests per window (default: 100)
RATE_LIMIT_MAX_REQUESTS=100

# ============================================
# Notes
# ============================================
# 1. AWS SES Setup:
#    - Verify your email address/domain in AWS SES Console
#    - Create IAM user with SES permissions
#    - See ../AWS_SES_SETUP.md for detailed instructions
#
# 2. MongoDB Setup:
#    - For local: Use MONGODB_URI above
#    - For Atlas: Replace with your Atlas connection string
#
# 3. Security:
#    - Never commit .env file to git
#    - Use strong JWT_SECRET in production
#    - Rotate AWS keys regularly
#
# 4. Optional Services:
#    - Cloudinary: Only needed for image uploads
#    - All other variables are required for basic functionality
